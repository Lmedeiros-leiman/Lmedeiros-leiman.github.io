import type { Dispatch, FC } from "preact/compat"
import type { StateUpdater } from "preact/hooks"


export const VideoPage : FC<{pageController : Dispatch<StateUpdater<string>> }> = ({pageController}) => {

  return (<></>)
}